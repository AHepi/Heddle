# example-incident

"Main was red on a clean checkout for two commits; a skill was deleted
without its inventory pin, and the gate never ran before landing."

(A seed note so the notes/ root exists and a first run has something to
read. Real incidents land here as one note each, with logs pasted in.)
