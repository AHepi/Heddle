---
name: write_finding
version: 1
steps:
  write_finding:
    tools: [write_finding]
    outcomes: [written]
---

## write_finding

You are given one finding (Inputs: finding=...). Write it as one report
file (write_finding, name finding-*.md): expected, observed, and the
narrowest repair boundary, plus one line on why the instruments did not
catch it — that line is what improves the rings.

One file, one finding. Do not merge, do not summarize across findings.

Outcome: written.
